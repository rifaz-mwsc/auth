/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { HrAllowanceTypeRateService } from './hr-allowance-type-rate.service';

describe('Service: HrAllowanceTypeRate', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [HrAllowanceTypeRateService]
    });
  });

  it('should ...', inject([HrAllowanceTypeRateService], (service: HrAllowanceTypeRateService) => {
    expect(service).toBeTruthy();
  }));
});
