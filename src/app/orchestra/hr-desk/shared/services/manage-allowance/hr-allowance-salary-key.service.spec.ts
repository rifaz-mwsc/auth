/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { HrAllowanceSalaryKeyService } from './hr-allowance-salary-key.service';

describe('Service: HrAllowanceSalaryKey', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [HrAllowanceSalaryKeyService]
    });
  });

  it('should ...', inject([HrAllowanceSalaryKeyService], (service: HrAllowanceSalaryKeyService) => {
    expect(service).toBeTruthy();
  }));
});
