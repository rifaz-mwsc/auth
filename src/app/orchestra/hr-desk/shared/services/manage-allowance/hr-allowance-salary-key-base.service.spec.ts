/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { HrAllowanceSalaryKeyBaseService } from './hr-allowance-salary-key-base.service';

describe('Service: HrAllowanceSalaryKeyBase', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [HrAllowanceSalaryKeyBaseService]
    });
  });

  it('should ...', inject([HrAllowanceSalaryKeyBaseService], (service: HrAllowanceSalaryKeyBaseService) => {
    expect(service).toBeTruthy();
  }));
});
