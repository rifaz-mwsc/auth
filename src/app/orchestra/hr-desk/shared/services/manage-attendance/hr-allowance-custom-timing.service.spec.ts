/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { HrAllowanceCustomTimingService } from './hr-allowance-custom-timing.service';

describe('Service: HrAllowanceCustomTiming', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [HrAllowanceCustomTimingService]
    });
  });

  it('should ...', inject([HrAllowanceCustomTimingService], (service: HrAllowanceCustomTimingService) => {
    expect(service).toBeTruthy();
  }));
});
